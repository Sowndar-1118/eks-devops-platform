apiVersion: v1
kind: Service

metadata:
  name: eks-platform-service

spec:
  type: NodePort

  selector:
    app: eks-platform

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30030
