apiVersion: v1
kind: Service

metadata:
  name: eks-platform-service

spec:
  selector:
    app: eks-platform

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

  type: LoadBalancer
